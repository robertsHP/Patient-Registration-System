{
  "name": "patient-registration-system",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/robertsHP/Patient-Registration-System",
  "author": "robertsHP",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "concurrently \"yarn --cwd server start\" \"yarn --cwd client start\"",
    "dev": "concurrently \"yarn --cwd client run dev\" \"yarn --cwd server run dev\"",
    "build": "concurrently \"yarn --cwd client build\" \"yarn --cwd server build\"",
    "test": "concurrently \"yarn --cwd client test\" \"yarn --cwd server test\""
  }
}
